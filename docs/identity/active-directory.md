# Active Directory

`DirectoryObject` representa forests, domains, OUs, computers, trusts, GPOs,
delegações, postura Kerberos/DNS e Certificate Services. O adapter registry
possui apenas códigos revistos e passivos. A implementação atual importa
fixtures sintéticas e não contacta domain controllers.
