# SBOMs

Upload a CycloneDX JSON document, review validation warnings and inspect
components/dependencies. The original remains private. A validated SBOM can be
linked to a release and used as a security-gate fact.
