# APIs

Create an API manually or upload a JSON OpenAPI document. Review the preview and
endpoint count before confirming. External references are unsupported. API 360
shows normalized paths and declared authentication without exposing credentials.
