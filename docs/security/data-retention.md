# Retenção de dados

Evidências são privadas e possuem `expires_at` para política futura. Findings e audit logs preservam histórico. A eliminação direta por Makefile está desativada; produção deve aplicar retenção auditada, legal hold, encriptação e lifecycle do object storage.
