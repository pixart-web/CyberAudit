# GCP

Organizations, folders, projects, IAM, VPC, Storage, compute, logging, GKE e
Security Command Center usam IDs externos estáveis. Service account keys não
são persistidas; apenas uma referência a Secret Manager é aceite.
