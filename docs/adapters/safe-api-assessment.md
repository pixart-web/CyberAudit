# Safe API Assessment

`cyberaudit.safe_api_assessment` is the closed contract for future safe runtime
checks. The current implementation evaluates declared posture only. Mutating
methods, fuzzing, enumeration and arbitrary headers are unavailable.
