# Session Security

`cyberaudit.session_security` reviews declared cookie/session controls. Tokens,
cookies and credentials are not accepted as output evidence or persisted in
logs. The adapter does not create or replay sessions.
