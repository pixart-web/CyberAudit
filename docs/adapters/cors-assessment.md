# CORS Assessment

`cyberaudit.cors_assessment` reviews declared origin and credential policy. It
does not send attacker-controlled origins to a target in Phase 5 and does not
interpret responses as executable HTML.
