# IaC Security

`cyberaudit.iac_security` reviews a declared representation and never applies
Terraform, CloudFormation, Kubernetes or other infrastructure. Cloud credentials
and arbitrary environment variables are not accepted.
