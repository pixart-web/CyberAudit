# Configuração pública

`cyberaudit.public_configuration` só aceita um caminho da enum: `robots.txt`, `security.txt`, `sitemap.xml` ou `manifest.json`. Não testa backups, repositórios, ficheiros sensíveis ou wordlists. Excertos são limitados, tratados como texto e redigidos.
