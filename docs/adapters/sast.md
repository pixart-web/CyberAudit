# SAST

`cyberaudit.sast_analysis` is a metadata-only baseline in Phase 5. No compiler,
interpreter, build script or external analyzer runs. Future SAST integrations
must use pinned tools in disposable, network-disabled runners.
