# Secret Detection Adapter

`cyberaudit.secret_detection` scans bounded supplied text with allowlisted
patterns. Output contains fingerprint, type, location, length, confidence and
mask only. Extra configuration fields are rejected.
