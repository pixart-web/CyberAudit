# Web Inventory

`cyberaudit.web_inventory` accepts URL targets and passive/low intensity. It uses
the central HTTP client, safe methods and bounded bytes/redirects. It inventories
headers, title, forms and internal links; it does not submit forms, run JavaScript
or follow external links.
