# Kubernetes Security

Clusters e objetos cobrem namespaces, workloads, RBAC, NetworkPolicy, ingress,
admission e metadados de secrets. Conteúdo de Secrets nunca é recolhido. A
arquitetura é de inventário read-only e não executa pods ou comandos.
